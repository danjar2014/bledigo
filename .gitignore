node_modules/
dist/
.next/
out/

# Sauvegardes et artefacts locaux
_backup-*/
*.zip
*.log
localities.json
prisma/dev.db*
bledigo-api/prisma/dev.db*

# Zone de transit des livraisons v2 : archives sources, deja integrees
Upgrade/

# Exports de tableaux generes pendant les echanges, sans valeur pour le code
table-*.csv

# Secrets : jamais commites, ils sont fournis par Render en production
.env
.env.local
.env.*.local

# Les migrations Prisma, elles, DOIVENT etre versionnees :
# `prisma migrate deploy` s appuie dessus au demarrage sur Render.
!bledigo-api/prisma/migrations/

# Configuration locale de l outillage Claude Code (chemins et ports de la machine)
.claude/

# Base de reference des migrations : se REGENERE, ne se conserve pas.
# Un instantane se perime des que le schema bouge, et construirait alors une
# base fausse sans rien signaler. Voir CLAUDE.md, « Construire une base neuve ».
baseline.sql
**/baseline.sql
